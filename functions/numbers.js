var a="7";
var b=8;
console.log(a+b);//if the values are numbers it add or else it will concate
console.log(typeof (a+b))// the result also will be a string
// typeof is used to know datatype of variables
//Math object
console.log(Math.round(7.8));//if decimal value is more or equla to 5 it gives next values
console.log(Math.ceil(7.2)); // whatever the decimal value it gives next value

console.log(Math.floor(7.8));// whatever the decimal value it gives same value
//Math object gives constant values also
console.log(Math.PI)

